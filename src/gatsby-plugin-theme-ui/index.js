import { roboto as theme } from "@theme-ui/presets"

export default {
  ...theme,
  styles: {
    ...theme.styles,
  },

  fonts: {
    body:
      '-apple-system,BlinkMacSystemFont,"Helvetica Neue",Arial,微軟正黑體,sans-serif',
    heading:
      '-apple-system,BlinkMacSystemFont,"Helvetica Neue",Arial,微軟正黑體,sans-serif',
  },
}
